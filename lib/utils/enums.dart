enum HistoryType { opened, uploaded, downloaded, signed, login, logout }

enum DocumentStatus { draft, pending, done, expired, archived, rejected }

enum DrawType { signature, initial }

enum BankStepType { icon, text, image }

enum SignOtp { init, approve, reject }

enum SignType { single, parallel, hierarchy }
